.clock-card {
  text-align: center;
  margin-bottom: 8rpx;
}

.clock-label {
  color: var(--muted);
  font-size: 24rpx;
}

.clock {
  display: block;
  margin-top: 8rpx;
  font-size: 72rpx;
  font-weight: 650;
  letter-spacing: 4rpx;
  color: var(--warm);
}

.delay-card {
  margin-top: 24rpx;
}
